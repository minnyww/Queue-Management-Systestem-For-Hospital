import React, { Component } from "react";


const modalDetailTimetable = props => {

    return (
        <div>

            {props.showDetailTimetableDescription()}

        </div>
    );
};

export default modalDetailTimetable;
